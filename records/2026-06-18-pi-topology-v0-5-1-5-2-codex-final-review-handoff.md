# v0.5.1.5 Final Review Handoff for Fresh Codex Session

date: 2026-06-18
handoff_from: Codex reviewer
target: fresh Codex session final verification
scope: `packages/pi-topology` v0.5.1.5 runtime alignment tail
latest_fix_commit: `ad0bd5b fix(pi-topology): v0.5.1.5 runtime alignment tail (P1 + P2 + P3)`
handoff_doc_to_review: `records/2026-06-18-pi-topology-v0-5-1-5-2-final-handoff.md`
constraint: no push / no publish / no real Ghostty launch / do not mutate `ekunCustomsWms`

---

## 0. Purpose

Run one fresh, independent final review of the v0.5.1.5 repair before any release readiness / push / publish decision.

This review exists because the previous dogfood attempt exposed multiple runtime alignment issues on first real use:

- clean/migrated workspaces mixed root `.pi/topology/*` and per-mission `.pi/topology/missions/<id>/*`
- `topology_write_artifact` wrote root artifacts while dashboard read per-mission artifacts
- spawn scripts/env vars pointed at root paths
- guard blocked writes without telling roles how to write correctly
- clean workspace init still used legacy root-only layout
- slash `/topology spawn hq` had a separate old root-path branch

The new session should verify these are actually closed, not merely green in isolated tests.

---

## 1. Current Known State

Expected HEAD:

```text
ad0bd5b fix(pi-topology): v0.5.1.5 runtime alignment tail (P1 + P2 + P3)
```

Expected dirty state before cleanup:

```text
 M records/2026-06-17-pi-topology-dogfood-run-smoke.md
```

That file is generated by dogfood/smoke evidence refresh. It is not part of the `ad0bd5b` code diff. The fresh reviewer should either ignore it as a local verification side effect or explicitly restore it before reporting a clean tree. Do not mix it into code review findings unless its content is intentionally being committed.

Relevant committed handoffs:

- `records/2026-06-18-pi-topology-v0-5-1-runtime-alignment-audit.md`
- `records/2026-06-18-pi-topology-v0-5-1-runtime-alignment-repair-handoff.md`
- `records/2026-06-18-pi-topology-v0-5-1-5-2-tail-handoff.md`
- `records/2026-06-18-pi-topology-v0-5-1-5-2-final-handoff.md`

---

## 2. Review Tasks

### A. Diff Review

Review `ad0bd5b` against the prior state. Focus on:

1. `topology_init_mission` tool now uses canonical `createMissionFlow`, not root-only `createMissionDraft`.
2. `/topology init` and bare clean init create:
   - `.pi/topology/mission-registry.json`
   - `.pi/topology/active-mission.json`
   - `.pi/topology/missions/<mission_id>/mission-card.json`
   - `.pi/topology/missions/<mission_id>/status-board.json`
   - per-mission ledgers, launch dir, artifacts dir
3. root files are compatibility mirrors, not source of truth.
4. `/topology spawn hq` from active Supervisor writes:
   - `missions/<id>/launch/hq.sh`
   - per-mission `PI_TOPOLOGY_MISSION_CARD`
   - per-mission `PI_TOPOLOGY_EVENT_LOG`
   - per-mission `PI_TOPOLOGY_INCIDENT_LOG`
   - per-mission `PI_TOPOLOGY_STATUS_BOARD`
   - per-mission `PI_TOPOLOGY_SESSIONS_LEDGER`
5. UI uses `resolveActiveMissionPaths` and does not separately prioritize `PI_TOPOLOGY_MISSION_CARD`.
6. `guard_block.evidence.inference` is a flat `string[]`.
7. tool descriptions no longer tell roles that artifacts are written only to root `.pi/topology/artifacts`.

### B. Test Coverage Review

Confirm these tests exist and are meaningful, not vacuous:

- `packages/pi-topology/test/unit/clean-init.test.ts`
- `packages/pi-topology/test/unit/per-mission-spawn-slash.test.ts`
- updated assertions in `packages/pi-topology/test/unit/extension.test.ts`
- `packages/pi-topology/test/integration/per-mission-runtime.test.ts`

Pay special attention to the deleted no-op slash spawn test mentioned in the final handoff. The replacement coverage should exercise the real command path: `commands.topology.handler("spawn hq", ctx)`.

### C. Manual Reasoning Smoke

Mentally trace these two workflows:

1. Clean workspace:
   `/topology <task>` -> current session becomes Supervisor -> mission exists under `missions/<id>` -> root is only mirror -> `topology_spawn_role` launches HQ with per-mission env.

2. Legacy workspace:
   `/topology migrate` -> registry + pointer + per-mission layout -> `/topology` resumes Supervisor -> tools/status/UI/session_start/heartbeat/spawn/artifacts all resolve per-mission canonical.

---

## 3. Verification Commands

Run from:

```text
cd /Users/yuantian/Documents/Coding/omp-topology-network/packages/pi-topology
```

Run these sequentially:

```bash
npm test
npm run test:integration
npm run dogfood
npm run smoke
```

Expected:

- `npm test` -> `324/324 pass`
- `npm run test:integration` -> `2/2 pass`
- `npm run dogfood` -> `1/1 pass`
- `npm run smoke` -> unit tests + typecheck + `npm pack --dry-run` pass

Important: **do not run `npm run test:integration` and `npm run dogfood` in parallel.**

Reason: `dogfood.test.ts` and `npm run dogfood` share a fixed temp run root:

```text
/var/folders/vq/dk3gntzd7dz529mp_57ybgyh0000gn/T/pi-topology-dogfood-smoke
```

Running them concurrently can produce a false failure like:

```text
ENOENT: no such file or directory, open .../missions/dogfood-smoke-2026-06-18-001/runtime-events.jsonl
```

This is a test-runner collision, not necessarily a product regression. If it happens, rerun dogfood alone before filing a finding.

---

## 4. Known Non-Blocking Residual

There is one known P3 UX leftover:

- In `commands.ts`, when the session is not Supervisor, `/topology spawn hq` guidance still points to root `.pi/topology/launch/topology-supervisor.sh`.

This does not block v0.5.1.5 because:

- main clean-init flow promotes the current session to Supervisor
- actual Supervisor spawn path now writes per-mission launch scripts/env
- `topology_spawn_role` tool path is per-mission

Treat it as follow-up UX cleanup unless the fresh review finds it affects a real supported workflow.

---

## 5. Reviewer Verdict Criteria

Approve if:

- no P1/P2 runtime alignment regressions are found
- clean init and migrated legacy both resolve per-mission canonical
- slash and tool spawn paths are per-mission
- artifact write/read, UI, session_start, heartbeat, guard all use the resolver/canonical paths
- all verification commands pass sequentially
- dirty smoke evidence is understood and not accidentally included as code work

Do not approve if:

- any tool writes root `.pi/topology/*` as canonical after registry exists
- clean workspace init still lacks registry/pointer/per-mission layout
- slash spawn hq can create root launch scripts in an active per-mission Mission
- tests only assert happy text output without checking actual paths/env vars
- dogfood fails when run alone after a clean sequential run

---

## 6. Suggested Final Report Shape

Use this format:

```text
Findings:
- P1/P2/P3 with file:line references, or "No blocking findings".

Verification:
- npm test: ...
- npm run test:integration: ...
- npm run dogfood: ...
- npm run smoke: ...

Decision:
- approve / hold

Notes:
- mention dirty dogfood evidence file if still present
- mention whether the known P3 slash guidance residual remains acceptable
```

End of handoff.
