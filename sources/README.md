# Sources

本目录保存 OMP拓扑网络的原始材料，分为两类：

- `cave/`：从 Cave 移入的专属实践目录。
- `project-handoffs/`：从 ekunAi / ekunCustomsWms 复制的真实项目 handoff 和 closeout。

这些文件保留原貌，作为证据和历史上下文；规范化后的入口在 `docs/`、`records/` 和 `templates/`。

